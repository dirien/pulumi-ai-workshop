name: ollama-gpu-server
description: Ollama GPU server on AWS with g5.xlarge instance
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
